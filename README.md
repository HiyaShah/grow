# grow
IOS E-commerce app that utilizes MVC, Firebase, and Stripe API.
Includes two apps: one for the admin (to add/remove products, see users) and one for the client (with login functionality, favorites, and checkout).
